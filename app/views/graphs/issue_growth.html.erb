<h2><%= l(:label_graphs_issue_growth) %></h2>
<% unless @issues.nil? %>
    <%= tag("embed", :width => "100%", :height => Integer(Setting.plugin_redmine_graphs['graph_height']), :type => "image/svg+xml", :src => url_for(:controller => 'graphs', :action => 'issue_growth_graph')) if @project.nil? %>
    <%= tag("embed", :width => "100%", :height => Integer(Setting.plugin_redmine_graphs['graph_height']), :type => "image/svg+xml", :src => url_for(:controller => 'graphs', :action => 'issue_growth_graph', :project_id => @project.id)) unless @project.nil? %>
<% else %>
    <%= render :partial => 'issues/list_simple', :locals => { :issues => @issues } %>
<% end %>

<% content_for :sidebar do %>
    <%= render :partial => 'issues/sidebar' %>
<% end %>
